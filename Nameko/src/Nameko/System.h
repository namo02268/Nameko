#pragma once

#include "Nameko/ECS.h"

namespace Nameko {
	/*
	class ECS;

	class System {
	protected:
		ECS* m_ecs;

	public:
		virtual ~System() = default;

	public:
		void SetECS(ECS* ecs) { this->m_ecs = ecs; }

		virtual void Init() = 0;
		virtual void Update(float dt) = 0;
		virtual void Draw() = 0;
	};
	*/
}